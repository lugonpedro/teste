type ExperienceProps = {
  key: number;
  title: string;
  header: string;
  place: string;
  desc: string;
  date: string;
  techs: string[];
};

type SkillProps = {
  image: string;
  title: string;
};

type ProjectProps = {
  image: any;
  title: string;
  description: string;
  alt: string;
  link: string;
};

type InputProps = {
  name: string;
  email: string;
  subject: string;
  message: string;
};
